[project]
name = "kickbaseproject"
version = "0.1.0"
description = "Data-driven Kickbase decision support"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32",
    "python-dotenv>=1.0",
    "beautifulsoup4>=4.15.0",
]

[tool.uv]
package = false
